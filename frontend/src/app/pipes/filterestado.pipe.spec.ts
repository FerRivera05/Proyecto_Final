import { FilterestadoPipe } from './filterestado.pipe';

describe('FilterestadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterestadoPipe();
    expect(pipe).toBeTruthy();
  });
});
