import { FilterlibrosporautorPipe } from './filterlibrosporautor.pipe';

describe('FilterlibrosporautorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlibrosporautorPipe();
    expect(pipe).toBeTruthy();
  });
});
