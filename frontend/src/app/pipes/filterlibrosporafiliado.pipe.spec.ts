import { FilterlibrosporafiliadoPipe } from './filterlibrosporafiliado.pipe';

describe('FilterlibrosporafiliadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlibrosporafiliadoPipe();
    expect(pipe).toBeTruthy();
  });
});
