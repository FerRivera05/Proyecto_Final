import { FilterareaPipe } from './filterarea.pipe';

describe('FilterareaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterareaPipe();
    expect(pipe).toBeTruthy();
  });
});
