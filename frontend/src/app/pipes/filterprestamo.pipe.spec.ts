import { FilterprestamoPipe } from './filterprestamo.pipe';

describe('FilterprestamoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterprestamoPipe();
    expect(pipe).toBeTruthy();
  });
});
