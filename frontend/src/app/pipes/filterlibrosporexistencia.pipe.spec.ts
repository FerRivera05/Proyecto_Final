import { FilterlibrosporexistenciaPipe } from './filterlibrosporexistencia.pipe';

describe('FilterlibrosporexistenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlibrosporexistenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
