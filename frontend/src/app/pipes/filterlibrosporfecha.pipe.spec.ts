import { FilterlibrosporfechaPipe } from './filterlibrosporfecha.pipe';

describe('FilterlibrosporfechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlibrosporfechaPipe();
    expect(pipe).toBeTruthy();
  });
});
