import { FilterlibroPipe } from './filterlibro.pipe';

describe('FilterlibroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlibroPipe();
    expect(pipe).toBeTruthy();
  });
});
