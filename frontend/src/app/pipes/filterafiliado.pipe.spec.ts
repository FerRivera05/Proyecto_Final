import { FilterafiliadoPipe } from './filterafiliado.pipe';

describe('FilterafiliadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterafiliadoPipe();
    expect(pipe).toBeTruthy();
  });
});
