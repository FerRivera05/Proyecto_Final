import { FilterautorPipe } from './filterautor.pipe';

describe('FilterautorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterautorPipe();
    expect(pipe).toBeTruthy();
  });
});
