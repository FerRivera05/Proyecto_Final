import { FiltereditorialPipe } from './filtereditorial.pipe';

describe('FiltereditorialPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltereditorialPipe();
    expect(pipe).toBeTruthy();
  });
});
