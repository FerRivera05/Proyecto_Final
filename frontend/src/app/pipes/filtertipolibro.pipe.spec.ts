import { FiltertipolibroPipe } from './filtertipolibro.pipe';

describe('FiltertipolibroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertipolibroPipe();
    expect(pipe).toBeTruthy();
  });
});
